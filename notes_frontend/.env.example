# PUBLIC_INTERFACE
# VITE_NOTES_API_URL is the base URL for the notes backend (notes_sqlite_db).
# Example values:
# - http://localhost:8000/api
# - https://your-domain.tld/api
# If not set, the app will use a relative '/api' path.
VITE_NOTES_API_URL=
